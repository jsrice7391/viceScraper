

<div id="hero">

</div>





    <div class="row">
        <div class="container">
   {{#each articles}}
        <div class="col s12 m6">
            <div class="card blue-grey">
                <div class="card-content white-text">
                    <a href="article/{{title}}"><span class="card-title">{{title}}</span></a>
                    <p>{{subText}}</p>
                </div>
                <div class="card-action">
                    <p>{{author}}</p>
                    <a href="article/{{_id}}">READ THE ARTICLE</a>
                </div>
            </div>
          
        </div>
        {{/each}}
    </div>

        </div>